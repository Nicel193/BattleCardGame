export default class Timer {
    constructor(scene, time, callback) {
        // this.timeText = scene.add.text(1000, 35, '', { fontSize: '32px', fill: '#fff' });
        // this.timeText.setText('Time: ' + time);

        // this.timer = scene.time.addEvent({
        //     delay: 30000,
        //     callback: callback,
        //     callbackScope: this
        // });
    }

    update() {
        // if (this.timer.callback == undefined) return;

        // var remainingTime = Math.ceil((this.timer.delay - this.timer.getElapsed()) / 1000);
        // this.timeText.setText('Time: ' + remainingTime);
    }

    stop() {
        // this.timer.remove();
    }
} 